# M033: Import Existing GSD Markdown As Legacy Reference

Existing projects may already have GSD Markdown. Since the database remains canonical, import should treat these files as legacy reference material, not as authoritative writable state.

## Success Criteria

- Existing Markdown can be attached as reference context.
- Generated projections still avoid unsafe overwrite.
- Imported references are visible during DISCUSS/PLAN.

