# M041: Per-Phase Model Settings Polish

Phase model preferences exist globally and per project, but users need a clearer review path before execution starts.

## Success Criteria

- Phase model overrides are summarized in the execution handoff.
- Missing model choices are visible before starting work.
- Tests cover global default and project override display.
