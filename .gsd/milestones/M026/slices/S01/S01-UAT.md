# S01 UAT: Know What Is Unblocked

A plan has three tasks where `T2` depends on `T1` and `T3` is independent.

## Expected

- Before execution, `T1` and `T3` are next work.
- `T2` is blocked by `T1`.
- After `T1` is done with evidence, `T2` becomes next work.
