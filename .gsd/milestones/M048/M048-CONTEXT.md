# M048: Settings Model Policy Polish

Phase model routing exists globally and per project, but the relationship between settings defaults, project overrides, and task session execution should be clearer and easier to audit.

## Success Criteria

- Settings explains global phase model defaults compactly.
- Project overrides are easy to compare against global defaults.
- Task sessions continue to record the resolved execution model.
