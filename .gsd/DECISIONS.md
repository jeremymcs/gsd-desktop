# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | data | Planning source of truth | Repo-local database is canonical; Markdown files are generated projections | The wizard needs durable structured state across every turn, while Markdown keeps compatibility with file-based GSD workflows | No |
| D002 | M001 | data | Database location | Store planning data at `.gsd/gsd.db` in the root workspace | The plan belongs to the project, not only the desktop app profile | Yes - if project-local binary storage proves unworkable |
| D003 | M001 | convention | Database version control | Ignore `.gsd/gsd.db`; commit generated Markdown projections | Binary database churn is review-hostile, but projections are portable and readable | No |
| D004 | M001 | convention | Git ignore mutation | Add `.gsd/gsd.db` to `.gitignore` when creating the planning database | Preventing accidental DB commits is part of the storage contract | Yes - if the app moves DB storage outside repos |
| D005 | M001 | library | SQLite driver | Use `better-sqlite3` in the Electron main process | Current Electron runtime lacks `node:sqlite`, and synchronous transactions fit append-only planning events | Yes - if Electron later ships a stable built-in SQLite API |
| D006 | M001 | arch | Planning package boundary | Create shared `packages/gsd-planning` for domain model, SQLite store, validation, and projections | The GSD planning engine should be reusable outside the desktop UI | Yes - if the package boundary becomes premature |
| D007 | M001 | arch | Electron integration | Electron owns IPC and workspace wiring; planning domain logic stays in the shared package | Keeps the renderer/preload boundary narrow and avoids trapping GSD semantics in UI code | No |
| D008 | M001 | ux | Plan Builder surface | Add a separate top-level Plan Builder, with creation also available from New Thread | Planning has durable lifecycle state that would overload the plain composer | Yes - after usability feedback |
| D009 | M001 | pattern | Wizard interaction model | Deterministic phase state machine with one active question at a time | Typed persistence and progress require stable phase/question records | No |
| D010 | M001 | data | Turn memory | Save each committed user answer immediately as an append-only event and typed current state | Restarts must not lose a turn, and event history is needed for audit and recomputation | No |
| D011 | M001 | data | Phase record shape | Store both structured records and a phase transcript/event stream | Structured state powers UI and projections; transcripts preserve the exact question/answer trail | No |
| D012 | M001 | workflow | Research handling | Run research agent-side, then require a user approval checkpoint before planning | Users should confirm assumptions before they become plan structure | Yes - if trusted auto mode is added |
| D013 | M001 | data | Generated output lifecycle | Persist generated research/planning output as `draft/proposed`, then commit accepted output as events | Restarts during review must not lose proposed work, but canonical state must distinguish proposal from approval | No |
| D014 | M001 | projection | Projection authority | Markdown projections are generated read-only views, not import sources | Two-way sync would create conflict complexity and undermine database authority | No |
| D015 | M001 | projection | Projection timing | Regenerate stable projections automatically after committed state changes | Existing file-based GSD workflows should see current state without manual export | Yes - if writes become too noisy |
| D016 | M001 | projection | Projection writes | Write projections atomically and only when generated content changes | Scoped writes reduce noisy diffs and avoid trampling legacy files | No |
| D017 | M001 | safety | Existing `.gsd` adoption | Treat existing Markdown as legacy reference and require confirmation before overwriting | Silent overwrite would violate project safety rules and user trust | No |
| D018 | M001 | scope | Plan multiplicity | Support multiple named plans per root workspace with one active plan by default | Parallel initiatives are likely, and adding identity later would be expensive | Yes - if UI complexity is too high |
| D019 | M001 | data | Item identity | Keep DB UUIDs hidden in generated headers/comments and expose readable `M001/S01/T01` IDs | Humans and agents need readable stable labels; DB identity must survive renames and reordering | No |
| D020 | M001 | data | Numbering | Milestone/slice/task numbers are immutable once assigned; order is a separate field | Stable filenames and append-only history need anchors that do not move | No |
| D021 | M001 | data | Item deletion | Deleting/parking/removing records appends events and hides removed items from active projections by default | IDs must not be reused, and plan history must remain explainable | No |
| D022 | M001 | workflow | Parked work | Move parked work to a workspace-level idea pool linked to the source plan/item | Parked items often outlive the current milestone and should not clutter active projections | Yes - after idea-pool UX is proven |
| D023 | M001 | workflow | Injection during execution | Allow injection through a draft change, impact review, and approval before it enters the active queue | Real projects change, but dependency and boundary integrity must be protected | Yes - post-MVP execution policy may evolve |
| D024 | M001 | validation | Plan approval gate | Block plan approval on unresolved dependency/boundary conflicts | The boundary map is only useful if it prevents invalid downstream plans | No |
| D025 | M001 | validation | MVP validation depth | Validate dependency existence, cycles, upstream consumed outputs, and demo/must-have coverage | This catches structural failures without requiring semantic code-interface validation in the MVP | Yes - semantic validation can be added later |
| D026 | M001 | workflow | Execution ownership | Plan Builder owns lifecycle status and task queue; linked normal sessions execute tasks | Execution needs the existing conversation, tool timeline, and session runtime surface | Yes - after execution UX is built |
| D027 | M001 | workflow | First milestone completion | Ship `DISCUSS`, `RESEARCH`, `PLAN`, projections, and restart persistence; defer full execution automation | This proves the core planning promise without expanding into the whole lifecycle at once | No |
| D028 | M001 | testing | Verification scope | Include package tests plus Electron Playwright tests for create, restart, and projections | Desktop work is not done until verified on the real Electron surface | No |
| D029 | M001 | workflow | GSD prompt compatibility | Model the wizard stages on the existing GSD prompt corpus: project discussion, requirements, milestone discussion, research, roadmap planning, slice context, and validation gates | The UI should preserve the proven workflow semantics instead of inventing a parallel planning process | No |
| D030 | M001 | data | Requirements contract | Treat requirements as a first-class capability contract with stable `R###` IDs, status, owner, source, and validation state | Roadmaps, slices, and verification need traceable capability ownership, not only prose context | No |
| D031 | M001 | ux | Plan Builder visual language | Use a dark, split-pane agent workspace visual model with top mode navigation, left vertical rail, pane headers, compact outlines, and bottom composer-style controls | The Plan Builder should feel like a focused desktop workbench, not a generic form wizard or marketing dashboard | Yes - after design review on the real Electron surface |
